---
"$schema": https://json-schema.org/draft/2020-12/schema
"$id": https://dm-screen.local/schema/dmscreen.uischema.yaml

title: DM Screen Application Data Schema
description: Root can be either a Campaign or a single Encounter
type: object

campaigns:
  type: array
  scope: "#/properties/campaigns"
  description: Campaigns available to the DM Screen application
  # items:
  #   "$ref": "#/$defs/Campaign"
  # options:
  #   #detail: "GENERATED"
  #   detail:
  #     type: "Group"
  #     #scope: "#/$defs/Campaigns"
  #   showSortButtons: true
  #minItems: 0

# Campaign:
#   type: Group
#   #scope: "#/$defs/Campaign"
#   elements:
#     - type: Control
#       scope: "#/$defs/Campaign/properties/name"
#     - type: Control
#       scope: "#/$defs/Campaign/properties/description"
